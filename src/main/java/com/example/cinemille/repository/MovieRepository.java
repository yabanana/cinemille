package com.example.cinemille.repository;

import com.example.cinemille.model.Movie;

public interface MovieRepository extends GenericRepository<Movie, Long> {
    // Metodi specifici per Movie se necessari
}
