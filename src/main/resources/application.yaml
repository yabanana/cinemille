spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cinemille_db
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
jwt:
  secret: your_secret_key
  expiration: 3600000  # 1 ora in millisecondi
